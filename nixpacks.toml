# Node version comes from package.json `engines.node`.

[phases.build]
cmds = ["pnpm build"]

# Apply Drizzle migrations, then start the adapter-node server (binds to $PORT).
[start]
cmd = "node scripts/migrate.mjs && node build"
